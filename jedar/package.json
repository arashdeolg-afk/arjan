{
  "name": "jedar-ai",
  "version": "0.1.0",
  "private": true,
  "description": "Jedar AI: a calm, voice-first faith-guidance companion (Expo mobile app + Express server).",
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "postinstall": "npm --prefix server install && npm --prefix mobile install",
    "typecheck": "npm --prefix server run typecheck && npm --prefix mobile run typecheck",
    "test": "npm --prefix server test && npm --prefix mobile test",
    "doctor": "npm --prefix mobile run doctor",
    "server": "npm --prefix server run dev",
    "mobile": "npm --prefix mobile start"
  }
}
